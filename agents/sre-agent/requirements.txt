# AI SRE Agent Dependencies
anthropic>=0.30.0       # Claude API (the reasoning engine)
boto3>=1.34.0           # AWS SDK (the agent's "hands")
requests>=2.31.0        # HTTP requests (health checks, Slack)
python-dotenv>=1.0.0    # Environment variable management
pyyaml>=6.0.0           # Parse runbook YAML files
rich>=13.0.0            # Pretty terminal output for demos
